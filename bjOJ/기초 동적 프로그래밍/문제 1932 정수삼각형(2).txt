문제 1932 정수삼각형(2)

귀결되는 값 개수= 나눠지는 경우의 수

깊이에 따라 값을 저장함
마지지막 깊이에서 최대값을 사용

나눠지는 경우 마지막줄

마지막줄 첫번째 값에 떨어지는 경우

2번째 값에 떨어지는 경우

3번째 값에 떨어지는 경우


dp[N][i]= dp[N-1][i] + f[N][i];

dp[N][i+1]= dp[N-1][i] +f[N][i+1]


dp[N-1][]



1
1 1
1 1 1 

위와 같은 방법보다 더 쉬운 방법 있음 아래로 내려가면서 계산하는 것보다 
위로 올라가면서 귀결하는 방법이 있음
문제에서는 위에서 아래로 방향으로 계산하였지만 역으로 생각할 필요가 있음

->사고 전환